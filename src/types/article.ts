export type ArticleMeta = {
  slug: string;
  title: string;
  excerpt: string;
  date: string;
  category: string;
  readingTime: number;
};

export type ArticleFrontmatter = {
  title: string;
  excerpt: string;
  date: string;
  category: string;
};
